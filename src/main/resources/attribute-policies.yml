# Missing list fields (e.g. `allowedPrefilledStrategies) are considered as `any`. If you wish to not enable any
# value, define the field as an empty list (e.g. `allowedPrefilledStrategies: [ ]`)
# Missing `allow` fields (e.g. `allowAsDestination`) are considered as `false`
# Missing `enforce` fields (e.g. `enforcedUpdatable`) are considered as any -> do not enforce any value


urn:perun:user:attribute-def:def:login-namespace:einfra:
  displayName: E-infra login
  allowAsDestination: true
  allowedItemTypes:
    - LOGIN
  allowedPrefillStrategies:
    - LOGIN_ATTRIBUTE
  enforcedUpdatable: false
  enforcedRequired: true
  enforceLabels: true
  enforceHelp: true
  enforceError: true
  enforcedTexts:
    en:
      label: "E-INFRA Login"
      help: "Enter your E-infra login username"
      error: "Invalid login format"
    cs:
      label: "E-INFRA přihlašovací jméno"
      help: "Zadejte své E-infra přihlašovací jméno"
      error: "Neplatný formát přihlašovacího jména"

urn:perun:user:attribute-def:def:displayName:
  displayName: Full name
  allowAsDestination: true
  allowAsSource: true
  sourcePrefillStrategy: IDM_ATTRIBUTE
  allowedItemTypes:
    - TEXTFIELD
  allowedPrefillStrategies:
    - IDM_ATTRIBUTE
    - IDENTITY_ATTRIBUTE
  allowedSourceAttributes:
    - urn:perun:user:attribute-def:def:displayName
    - name
  enforcedUpdatable: false
  enforcedRequired: true
  enforceLabels: true
  enforceHelp: true
  enforcedTexts:
    en:
      label: "Full name"
      help: "Please enter your full name"
    cs:
      label: "Jmeno a prijmeni"
      help: "Prosim zadejte sve jmeno a prijmeni"

name:
  displayName: Full name (identity)
  allowAsSource: true
  sourcePrefillStrategy: IDENTITY_ATTRIBUTE

urn:perun:member:attribute-def:def:address:
  displayName: Member address
  allowAsDestination: true
  allowedItemTypes:
    - TEXTFIELD
  enforcedPrefillOptions:
    - prefillStrategyType: IDM_ATTRIBUTE
      sourceAttribute: urn:perun:member:attribute-def:def:address
      options:
        test: test
  enforcedUpdatable: false
  enforcedRequired: true
  enforceLabels: true
  enforceHelp: true
  enforcedTexts:
    en:
      label: "Address"
      help: "Please enter your home address"
    cs:
      label: "Adresa"
      help: "Prosim zadejte svou adresu"
